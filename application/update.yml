Validation using JsonSchema for checking the update
---
tags:
  - update
parameters:
  - name: body
    in: formData
    required: true
    schema:
      id: User
      required:
        - type
        - val
        - n
        - code
      properties:
        type:
          type: string
          default: "0"
        val:
          type: string
          default: "0"
        n:
          type: string
          default: "0"
        code:
          type: string
          default: "0"
